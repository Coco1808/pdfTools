# PDF Tools 产品需求文档（PRD）


| 文档信息 | 内容                |
| ---- | ----------------- |
| 产品名称 | PDF Tools         |
| 版本   | v1.0              |
| 文档状态 | 草稿                |
| 更新日期 | 2026-08-06        |
| 目标平台 | Web 端（桌面优先，兼容移动端） |


---

## 1. 背景与目标

面向个人与中小企业用户，提供轻量、本地化优先的 PDF 处理能力。首期聚焦两类高频场景：

1. **多份 PDF 合并为一份**
2. **发票类 PDF 金额识别与按类型汇总**

产品定位：精美、灵动、操作路径短，上传后即可完成核心任务，减少学习成本。

### 1.1 成功标准

- 用户可在 3 步内完成「合并」或「发票汇总」
- 合并结果可下载，页序可控
- 发票汇总可按发票类型展示合计，并支持导出
- 页面视觉现代、动效克制、反馈及时，主观体验评分目标 ≥ 4.5/5（内测）

---

## 2. 用户与场景

### 2.1 目标用户


| 角色      | 典型诉求                      |
| ------- | ------------------------- |
| 财务 / 行政 | 多张发票合并归档；按增值税专用/普通等类型统计金额 |
| 自由职业者   | 报销前快速核对发票总额               |
| 普通办公用户  | 合同、报告等多 PDF 合并为一个文件       |


### 2.2 核心场景

**场景 A：合并报销附件**  
用户上传多份 PDF（发票、行程单等），调整顺序后一键合并并下载。

**场景 B：发票金额汇总**  
用户上传多张发票 PDF，系统识别金额与类型，按类型汇总，展示总计，可导出结果。

---



## 3. 功能需求



### 3.1 功能总览


| 模块        | 优先级 | 说明                |
| --------- | --- | ----------------- |
| PDF 合并    | P0  | 多文件上传、排序、合并、下载    |
| 发票识别与汇总   | P0  | 识别金额/类型，按类型合计     |
| 首页与导航     | P0  | 双入口清晰引导           |
| 处理反馈与错误提示 | P0  | 进度、成功、失败可读可恢复     |
| 结果导出      | P1  | 汇总表导出 Excel / CSV |
| 历史记录（本地）  | P2  | 浏览器本地记录最近操作（可选）   |


---



### 3.2 PDF 合并（P0）



#### 3.2.1 功能描述

用户上传多个 PDF，调整页序/文件顺序后合并为一个 PDF 并下载。

#### 3.2.2 详细需求


| ID   | 需求      | 说明                                                 |
| ---- | ------- | -------------------------------------------------- |
| M-01 | 多文件上传   | 支持拖拽上传与点击选择；仅接受 `.pdf`                             |
| M-02 | 数量与大小限制 | 单次最多 **20** 个文件；单文件 ≤ **50MB**；合计 ≤ **100MB**（可配置） |
| M-03 | 文件列表    | 展示文件名、大小、页数（若可解析）、缩略预览（首屏可选）                       |
| M-04 | 排序      | 支持拖拽排序；上移/下移；删除单个文件                                |
| M-05 | 合并执行    | 一键合并，展示进度（上传中 / 处理中 / 完成）                          |
| M-06 | 结果下载    | 合并成功后提供下载；默认文件名 `merged-YYYYMMDD-HHmmss.pdf`       |
| M-07 | 清空重置    | 支持清空列表重新开始                                         |
| M-08 | 异常处理    | 损坏 PDF、加密 PDF、超限等给出明确中文提示与可操作建议                    |




#### 3.2.3 交互要点

- 空态：大面积拖拽区 + 简短引导文案 + 「选择文件」按钮
- 有文件：列表区为主，底部固定操作栏（合并 / 清空）
- 合并中：禁止重复提交，展示进度条与预计状态文案
- 完成后：成功动效 + 主按钮「下载」+ 次按钮「继续合并」

---



### 3.3 发票类型识别与金额汇总（P0）



#### 3.3.1 功能描述

用户上传发票类 PDF，系统解析发票关键信息，按发票类型汇总金额，并展示总计。

#### 3.3.2 识别字段（首期）


| 字段        | 必填  | 说明                                             |
| --------- | --- | ---------------------------------------------- |
| 发票类型      | 是   | 如：增值税专用发票、增值税普通发票、电子发票（普通）、电子发票（专用）、火车票、行程单、其他 |
| 价税合计 / 金额 | 是   | 优先取「价税合计」；无则取「金额」+「税额」求和                       |
| 发票号码      | 否   | 用于去重与核对                                        |
| 开票日期      | 否   | 列表展示                                           |
| 销售方名称     | 否   | 列表展示                                           |
| 识别置信度     | 否   | 低置信度标记「需核对」                                    |




#### 3.3.3 详细需求


| ID   | 需求    | 说明                               |
| ---- | ----- | -------------------------------- |
| I-01 | 多发票上传 | 同合并模块上传交互；支持多文件                  |
| I-02 | 自动解析  | 上传后自动识别；支持「重新识别」                 |
| I-03 | 结果列表  | 每张发票一行：类型、金额、日期、号码、状态（成功/失败/需核对） |
| I-04 | 人工修正  | 用户可编辑类型、金额；修正后实时重算汇总             |
| I-05 | 按类型汇总 | 按发票类型分组展示：数量、金额合计                |
| I-06 | 总计    | 展示全部发票金额总和、成功识别数 / 失败数           |
| I-07 | 去重提示  | 相同发票号码出现两次时提示，不默认删除              |
| I-08 | 导出    | 导出汇总表（CSV，P1 可扩展 Excel）          |
| I-09 | 失败兜底  | 识别失败可手动填写金额与类型后再参与汇总             |




#### 3.3.4 汇总展示示例

```
增值税专用发票    3 张    ¥12,580.00
增值税普通发票    5 张    ¥3,210.50
电子发票（普通）  2 张    ¥890.00
────────────────────────────────
合计             10 张   ¥16,680.50
```



#### 3.3.5 业务规则

1. 金额统一保留 **2** 位小数，使用人民币符号展示
2. 无法识别类型时归入「其他」，并标记需核对
3. 加密或扫描件模糊导致失败时，允许用户手动补录
4. 首期以**文本型 PDF / 电子发票**为主；纯图片扫描件识别为增强项（见非功能与后续规划）

---



### 3.4 通用能力


| ID   | 需求         | 说明                                 |
| ---- | ---------- | ---------------------------------- |
| C-01 | 路由导航       | `/` 首页；`/merge` 合并；`/invoice` 发票汇总 |
| C-02 | 响应式        | 桌面优先；平板/手机可用（列表改为纵向卡片）             |
| C-03 | 无账号        | 首期不强制登录；文件处理完可丢弃                   |
| C-04 | 隐私提示       | 明确告知文件用途、保留时长、是否上传服务器              |
| C-05 | Toast / 通知 | 成功、警告、错误统一反馈组件                     |


---



## 4. 非功能需求



### 4.1 性能


| 指标                       | 目标            |
| ------------------------ | ------------- |
| 首页首屏                     | ≤ 2s（宽带环境）    |
| 合并 10 个普通 PDF（合计 ≤ 20MB） | ≤ 15s         |
| 发票识别单文件                  | ≤ 8s（文本型 PDF） |
| 接口超时                     | 默认 60s，可配置    |




### 4.2 安全与隐私

- 传输使用 HTTPS
- 服务端临时文件处理完成后删除（建议保留 ≤ 1 小时，定时清理）
- 不将用户文件用于训练或第三方分享
- 上传校验 MIME / 扩展名 / 魔数，防止恶意文件
- 限制请求频率，防止滥用



### 4.3 可用性与体验

- 关键操作有加载态、空态、错误态、成功态
- 动效时长建议 200–400ms，遵循「有反馈、不抢戏」
- 关键文案中文、口语化、可行动（告诉用户下一步做什么）
- 支持键盘可达（按钮、对话框）



### 4.4 兼容性

- 浏览器：Chrome / Edge / Firefox / Safari 近两个大版本
- 分辨率：≥ 1280px 完整体验；≥ 375px 可用

---



## 5. 技术架构建议



### 5.1 总体架构

```
浏览器（Vite + React）
        │  HTTPS / multipart
        ▼
后端 API（FastAPI）
        │
   ┌────┴────┐
   │         │
PDF 合并   发票解析
(pypdf)   (pdfplumber + 规则 / OCR 可选)
   │         │
   └────┬────┘
        ▼
  临时存储（本地磁盘 / 对象存储）
```



### 5.2 前端技术选型


| 项          | 选型                           | 说明                  |
| ---------- | ---------------------------- | ------------------- |
| 构建         | Vite                         | 开发体验快               |
| 框架         | React 18+ + TypeScript       | 类型安全                |
| 路由         | React Router                 | 多页面                 |
| 样式         | CSS Modules 或 Tailwind CSS   | 建议 Tailwind 加速精美 UI |
| 动效         | Framer Motion                | 列表排序、页面切换、成功反馈      |
| 文件上传       | 原生 + 拖拽；可配合 `react-dropzone` |                     |
| 状态         | Zustand 或 React Query        | 上传任务与结果缓存           |
| PDF 预览（可选） | pdf.js                       | 合并前预览               |




### 5.3 后端技术选型（建议）

推荐 **Python + FastAPI**：


| 能力      | 库 / 方案                   |
| ------- | ------------------------ |
| Web 框架  | FastAPI                  |
| PDF 合并  | pypdf / PyMuPDF          |
| 文本提取    | pdfplumber / PyMuPDF     |
| 发票字段解析  | 正则 + 版面规则（电子发票结构化文本）     |
| OCR（后续） | PaddleOCR / 云 OCR（扫描件增强） |
| 校验      | pydantic                 |
| 任务清理    | 后台定时任务删除临时文件             |


> 备选：Node.js + Express/NestJS + `pdf-lib`，前后端同语言；但发票中文版面解析生态上 Python 更成熟，故首期推荐 FastAPI。



### 5.4 建议 API 设计



#### `POST /api/merge`

- 请求：`multipart/form-data`，字段 `files[]`（按顺序）
- 响应：合并后的 PDF 文件流，或 `{ downloadUrl, fileName }`



#### `POST /api/invoice/analyze`

- 请求：`multipart/form-data`，字段 `files[]`
- 响应示例：

```json
{
  "items": [
    {
      "id": "uuid",
      "fileName": "发票1.pdf",
      "invoiceType": "增值税专用发票",
      "amount": 1258.0,
      "invoiceNumber": "12345678",
      "invoiceDate": "2026-07-01",
      "sellerName": "某某科技有限公司",
      "confidence": 0.92,
      "status": "success"
    }
  ],
  "summary": [
    { "invoiceType": "增值税专用发票", "count": 1, "totalAmount": 1258.0 }
  ],
  "grandTotal": 1258.0,
  "successCount": 1,
  "failedCount": 0
}
```



#### `GET /api/health`

- 健康检查



### 5.5 目录结构建议（示意）

```
pdf-tools/
├── web/                 # Vite + React 前端
│   ├── src/
│   │   ├── pages/
│   │   ├── components/
│   │   ├── api/
│   │   ├── hooks/
│   │   └── styles/
│   └── package.json
├── server/              # FastAPI 后端
│   ├── app/
│   │   ├── api/
│   │   ├── services/
│   │   └── utils/
│   └── requirements.txt
└── 需求文档.md
```

---



## 6. 页面与交互设计要求



### 6.1 设计原则

1. **一屏一主任务**：首页只做品牌与两大入口，不堆功能
2. **品牌即第一印象**：产品名「PDF Tools」在首屏有明确视觉权重
3. **真实场景氛围**：以文档/票据质感为视觉锚点（轻纹理、柔和光影），避免空洞纯色块
4. **动效服务反馈**：拖入高亮、列表排序、处理进度、成功完成四处重点动效即可
5. **少卡片化**：列表与操作区以版面节奏组织；卡片仅用于可交互结果块
6. **色彩方向**：建议冷灰纸张底 + 深墨字 + 单一强调色（如松柏绿或靛蓝），避免紫渐变、奶油衬线、报纸排版等模板感



### 6.2 页面清单


| 页面               | 内容                             |
| ---------------- | ------------------------------ |
| 首页 `/`           | 品牌名、一句价值主张、合并 / 发票汇总双 CTA、背景氛围 |
| 合并页 `/merge`     | 上传区、文件列表（可拖拽排序）、操作栏、结果态        |
| 发票汇总页 `/invoice` | 上传区、识别列表（可编辑）、类型汇总面板、导出        |
| 通用               | Header（Logo + 导航）、Footer（隐私说明） |




### 6.3 关键动效清单（最少实现）

1. 文件拖入上传区时的边框/背景呼吸反馈
2. 列表项拖拽排序时的位移动画
3. 处理中的进度条或骨架屏
4. 完成后的轻量成功反馈（勾选/淡入结果区）

---



## 7. 验收标准



### 7.1 PDF 合并

- [ ] 可上传 ≥ 2 个合法 PDF 并合并下载
- [ ] 拖拽排序后，合并页序与列表一致
- [ ] 超限、非 PDF、损坏文件有明确错误提示
- [ ] 合并过程不可重复点击导致多次提交



### 7.2 发票汇总

- [ ] 上传多张电子发票 PDF，能解析出金额与类型（样本集正确率目标 ≥ 85%，文本型）
- [ ] 按类型汇总金额正确（与手动修正后的数据一致）
- [ ] 手动修改金额/类型后，汇总实时更新
- [ ] 识别失败条目可手动补录并计入合计
- [ ] 可导出 CSV，字段完整



### 7.3 体验

- [ ] 空态 / 加载 / 成功 / 失败四态齐全
- [ ] 桌面端视觉完整、动效流畅（无明显卡顿）
- [ ] 移动端可完成上传与下载主流程

---



## 8. 里程碑建议


| 阶段  | 内容                           | 预估      |
| --- | ---------------------------- | ------- |
| M1  | 前端骨架 + 首页/合并页 UI + 合并 API 打通 | 1 周     |
| M2  | 发票解析规则 + 汇总页 + 人工修正          | 1–1.5 周 |
| M3  | 导出、错误处理、动效打磨、隐私清理            | 0.5–1 周 |
| M4  | 扫描件 OCR（可选增强）                | 后续      |


---



## 9. 风险与对策


| 风险          | 影响     | 对策              |
| ----------- | ------ | --------------- |
| 发票版式多样，识别不准 | 汇总错误   | 人工修正入口 + 低置信度标记 |
| 扫描件/图片 PDF  | 首期识别率低 | 明确范围；后续加 OCR    |
| 大文件合并慢/内存高  | 超时失败   | 限制大小；流式/分片；超时提示 |
| 加密 PDF      | 无法处理   | 提示用户先解密         |


---



## 10. 首期范围外（Out of Scope）

- 用户登录 / 云端历史同步
- PDF 拆分、压缩、加水印、转 Word
- 发票真伪查验、税务局接口对接
- 多人协作与权限系统
- 原生 App

---



## 11. 附录



### 11.1 名词解释


| 名词      | 说明                |
| ------- | ----------------- |
| 价税合计    | 发票上含税总金额          |
| 置信度     | 系统对识别结果的可信程度估计    |
| 文本型 PDF | 可选中复制文字的 PDF，非纯图片 |




### 11.2 后续可扩展

- PDF 拆分、压缩、旋转
- 发票 OCR（扫描件）
- 汇总图表可视化
- 批量模板字段导出到财务软件格式

---

**文档结束**